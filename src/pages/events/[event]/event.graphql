query getEvent($data: EventWhereUniqueInput!) {
    event(where: $data) {
        id
        name
        startDate
        endDate
        displayDate
        managers
        registrationCutoff
        eventGroup {
            name
        }
    }
}
