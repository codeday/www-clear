mutation EventAddMutation ($data: EventCreateInput!) {
    createEvent(data: $data) {
    id
}

}
query getEvents {
    events{
        id
        name
        displayDate
        eventGroup {
            name
        }
    }
}
