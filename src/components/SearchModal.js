import React, { useRef, useState } from 'react';
import { useHotkeys } from "react-hotkeys-hook";
import { InputGroup, InputLeftAddon, Box, Option } from '@codeday/topo/Atom';
import { Select } from 'chakra-react-select';
import { UiSearch } from '@codeday/topocons/Icon'

export default function SearchBar({ items, icons, ...props }) {
  return ( <></>
      )
}
